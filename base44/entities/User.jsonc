{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "worker"
      ],
      "default": "worker",
      "description": "Rol del usuario: admin = control total, worker = solo sus proyectos asignados"
    }
  },
  "required": []
}